# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://your-site.vercel.app

# Public Contact Links (Optional)
# Used to avoid hardcoding personal identifiers in the repo.
# NEXT_PUBLIC_CONTACT_EMAIL=you@example.com
# NEXT_PUBLIC_LINKEDIN_URL=https://www.linkedin.com/in/your-handle/

# GitHub API (Optional - for higher rate limits)
# Create at: https://github.com/settings/tokens
# Permissions needed: public_repo (read-only)
# GITHUB_TOKEN=your_github_personal_access_token_here

# Analytics (Optional)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Vercel Analytics (Optional - auto-detected on Vercel)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# PostHog Analytics (Optional)
# Get your project API key from: https://posthog.com (Settings > Project API Key)
# The key is safe to expose client-side as it's write-only
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_project_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Security Note: Never commit .env.local to git!
# It's already in .gitignore

# Spotify "Now Playing" (Optional)
# Used by /api/spotify/now-playing to show your currently playing track on /links.
# Requires a Spotify app + refresh token with scope: user-read-currently-playing
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# SPOTIFY_REFRESH_TOKEN=
